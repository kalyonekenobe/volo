import { Outlet } from "react-router";

const WithNavbarTemplate = () => {
  // todo: make navbar and insert it here
  return (
    <>
      Navbar
      <Outlet />
    </>
  );
};

export default WithNavbarTemplate;
